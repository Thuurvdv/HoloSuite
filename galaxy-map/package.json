{
  "name": "@holosuite/galaxy-map",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run test && npm run build"
  },
  "devDependencies": {
    "handlebars": "^4.7.9",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
